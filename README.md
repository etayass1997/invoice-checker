# בדיקת חשבוניות — הרצה

## התקנה (פעם ראשונה)
```bash
pip install -r requirements.txt
```

## הרצה
```bash
streamlit run app.py --server.port 8501
```

גש בדפדפן לכתובת: http://localhost:8501
או ברשת הפנימית: http://[IP-של-השרת]:8501
